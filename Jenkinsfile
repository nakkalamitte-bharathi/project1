pipeline {
    agent any
    stages {
        stage('compile') {
            steps {
       	      	echo 'Enter Branch1 Build'
		}
        }
        stage('build'){
            steps{
                echo 'Enter Branch1 Build'
            
            }
        }
        stage('archive'){
            steps{
                echo 'Enter Branch1 Archive'
            }
        }
    }
}
